//  1
const tableElm = document.getElementById("age-table");

//  2
const labels = document.querySelectorAll("#age-table label");

// 3
const firstTd = document.querySelector("#age-table td:first-child");

// 4
const formElm = document.querySelector("form[name='search']");

// 5
const firstInput = document.querySelector(
  "form[name='search'] input:first-child"
);

// 6
const lastInput = document.querySelector(
  "form[name='search'] input:last-child"
);
